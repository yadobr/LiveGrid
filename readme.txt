1) Using in "document.onload()" or "$(document).ready()"
2) Wrap your table in div with class lg-wrap. You may set a new wrap width. If you need it. Div width should be less than table width. Like as Excel behaviour.
3) <th> in first <tr> using as headings for cols resize